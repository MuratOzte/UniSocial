import React from 'react';

const EventCard = ({ event, onJoin }) => {
    if (!event) return null;
    console.log(event);

    return (
       <div>
            
       </div>
    );
};

export default EventCard;
